% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiles.R
\name{bluertopo_tile_polygons}
\alias{bluertopo_tile_polygons}
\title{Retrieve all BlueTopo tile polygons}
\usage{
bluertopo_tile_polygons(
  cache_dir = bluertopo_cache_dir(),
  refresh = "if_stale",
  quiet = FALSE
)
}
\arguments{
\item{cache_dir}{A non-empty character path for the package cache. The
session-temporary default avoids writing to the user's home directory. Set
an explicit path to reuse catalogs, source files, and VRTs across sessions.}

\item{refresh}{A character scalar controlling catalog access:
\code{"if_stale"}, \code{"never"}, or \code{"always"}. \code{"never"} requires an existing
cached catalog and performs no catalog request.}

\item{quiet}{A length-one logical suppressing routine informational messages.}
}
\value{
A \code{terra::SpatVector} containing all current BlueTopo tile polygons
and their standardized catalog metadata, including tile ID, native
resolution, UTM zone, source URLs, and expected SHA-256 checksums.
}
\description{
Downloads or reuses the current NOAA BlueTopo tile-scheme catalog and
returns every published tile footprint. This function does not require an
area of interest and does not download any bathymetry rasters.
}
\examples{
\donttest{
\dontshow{
host <- "noaa-ocs-nationalbathymetry-pds.s3.amazonaws.com"
bluertopo_tile_polygons <- function(...) if (!is.null(curl::nslookup(host, error = FALSE)))
  tryCatch(
    bluertopo::bluertopo_tile_polygons(...),
    bluertopo_error = function(e) NULL
  )
}
tile_polygons <- bluertopo_tile_polygons()
}
}
